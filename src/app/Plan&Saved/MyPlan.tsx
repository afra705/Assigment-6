import React from 'react';
import { IWorkout } from '../type/type';

const MyPlan = ({workout}:{workout:IWorkout}) => {
    return (
        <div>
            
        </div>
    );
};

export default MyPlan;